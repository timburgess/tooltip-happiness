Tooltip exercise:

1. Implement the function 'spawnTooltip()' in tooltip.js. Functionality should match the descriptions provided for each specific link in the html file provided. Note the html template in index.html.

2. Style the tooltips to match the example in example.png.
